return
{
    {
        "voldikss/vim-translator",
        -- cmd = "",
        keys = {
            { "<leader>t", "<cmd>Translate<cr>", desc = "Translate", mode = { "n" } },
            -- opts = {
            --   -- add your options that should be passed to the setup() function here
            --   position = "right",
        },
    }
}
